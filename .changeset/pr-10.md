---
"@myarchitectai/mcp": minor
---

The `usage_summary` tool now reports a `failedGenerations` count alongside successful generations, and `lastKnownBalance` is updated from API error responses as well as successful calls.
