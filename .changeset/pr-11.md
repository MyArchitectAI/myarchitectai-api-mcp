---
"@myarchitectai/mcp": minor
---

The `preview_image` and `save_image` tools now accept inline `data:` URIs and local file paths (absolute, `~`-prefixed, or `file://`) in addition to public HTTPS URLs. Images from these sources are loaded without any network request.
