---
"@myarchitectai/mcp": minor
---

The `usage_summary` tool now includes a masked API key fingerprint (e.g. `…6789`) in both its structured output and human-readable text, so you can confirm which key is active without exposing the full secret.
